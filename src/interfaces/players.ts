import { Player } from "./player";

/**
 * Players module state
 *
 * @export
 * @interface Players
 */
export interface Players {
  /**
   * List of players
   *
   * @type {Player[]}
   * @memberof Players
   */
  list: Player[];
}
